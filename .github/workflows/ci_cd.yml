name: Docker CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    env:
      HOST: ${{ secrets.HOST }}
      PORT: ${{ secrets.PORT }}
      mongo_URL: ${{ secrets.MONGO_URL }}
    steps: 
    - 
      name: Check out Repo
      uses: actions/checkout@v2

    - 
      name: Login to Docker Hub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
    
    - 
      name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1
      id: buildx
    
    - 
      name: Build and push
      id: docker_build
      uses: docker/build-push-action@v2
      with:
        context: ./
        file: ./Dockerfile
        push: true
        tags: ${{ secrets.DOCKER_HUB_USERNAME }}/go-backend:latest

  test:
    runs-on: ubuntu-latest
    env:
      HOST: ${{ secrets.HOST }}
      PORT: ${{ secrets.PORT }}
      mongo_URL: ${{ secrets.MONGO_URL }}
      JWT_TOKEN: ${{ secrets.JWT_TOKEN }}
      username: ${{ secrets.DOCKER_HUB_USERNAME }}
      password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
    steps:
      - uses: actions/checkout@v2
      - name: Go unit testing
        run: |
          docker pull tikokito/go-backend:latest
          docker run -p 80:80 --entrypoint sh tikokito/go-backend:latest
          
          go test
  


# cd server 
# go build -o run-server
# ./run-server &
# echo "something has has been changed"
# go test
  deplyoment:
      runs-on: ubuntu-latest
      
      steps:
      -
        name: Check out Repo
        uses: actions/checkout@v2